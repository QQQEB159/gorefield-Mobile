name: Release
on: workflow_dispatch

permissions: write-all

jobs:
  build-android:
    uses: ./.github/workflows/android.yml
  build-ios:
    uses: ./.github/workflows/ios.yml

  Releaser:
    needs: [build-android, build-ios]
    runs-on: ubuntu-latest
    steps:
      - name: Download Android Build
        uses: actions/download-artifact@main
        with:
          name: Gorefield Android
          path: ${{ github.workspace }}

      - name: Move Android File
        run: mv "${{ github.workspace }}/gorefield-release.apk" "${{ github.workspace }}/Gorefield-Android.apk"

      - name: Download iOS Build
        uses: actions/download-artifact@main
        with:
          name: Gorefield iOS
          path: ${{ github.workspace }}

      - name: Move iOS File
        run: mv "${{ github.workspace }}/gorefield.ipa" "${{ github.workspace }}/Gorefield-iOS.ipa"

      - name: Publish The Release
        uses: softprops/action-gh-release@v2
        with:
          name: "v2.0.0"
          tag_name: "v2.0.0"
          prerelease: false
          files: |
            ${{ github.workspace }}/*.apk
            ${{ github.workspace }}/*.ipa
